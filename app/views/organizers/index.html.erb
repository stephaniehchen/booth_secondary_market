<div class="page-header">
  <h1>
    Organizers
    <a href="/organizers/new" class="btn btn-lg btn-success">New Organizer</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Name</th>
        <th>Url</th>
        <th>Actions</th>
      </tr>

      <% @organizers.each do |organizer| %>
      <tr>
        <td><%= organizer.name %></td>
        <td><%= organizer.url %></td>
        <td>
          <a href="/organizers/<%= organizer.id %>" class="btn btn-primary">Show</a>
          <a href="/organizers/<%= organizer.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_organizer/<%= organizer.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
